# Dependencies
/node_modules

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Kamal deployment - .env may contain secrets
.env

# Note: build/ is NOT gitignored because Kamal clones the repo to build
# the Docker image. The build output must be committed for deployment.
# Run `npm run build` before committing to update the docs.
